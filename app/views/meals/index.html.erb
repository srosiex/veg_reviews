<h1>Meals Index</h1>

<% @meals.each do |m| %>
   <h2> <%= m.name %> </h2>
    <%= m.description %><br>
    <%= m.restaurant.name %>
    <%= link_to "Write a review", new_meal_review_path(m.id) %>
    <% end %>